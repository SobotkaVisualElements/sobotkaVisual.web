---
interface Props {
	size?: 'md' | 'sm';
	href?: string;
}
const { size = 'md', href = '/' } = Astro.props;
const h = size === 'md' ? 'h-8' : 'h-7';
---

<a href={href} class="inline-flex items-center">
   <img src="/mark.svg" alt="Sobotka Visual Creations" class:list={['w-auto', h]} />
</a>
